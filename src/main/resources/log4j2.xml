<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="30">

    <!-- === Appenders (salidas de log) === -->
    <Appenders>

        <!-- Consola con colores (solo desarrollo) -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="%style{%d{yyyy-MM-dd HH:mm:ss.SSS}}{cyan} %highlight{%-5p} %style{[%t]}{magenta} %style{%c{1.}}{yellow} - %msg%n%throwable" />
        </Console>

        <!-- Archivo de log rotativo -->
        <RollingFile name="FileAppender"
                     fileName="logs/app.log"
                     filePattern="logs/app-%d{yyyy-MM-dd}-%i.log.gz">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss.SSS} %-5p [%t] %c{1.} - %msg%n" />
            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
                <SizeBasedTriggeringPolicy size="10MB" />
            </Policies>
            <DefaultRolloverStrategy max="10" />
        </RollingFile>

        <!-- Formato JSON para sistemas de monitoreo -->
        <RollingFile name="JsonAppender"
                     fileName="logs/app-json.log"
                     filePattern="logs/app-json-%d{yyyy-MM-dd}-%i.json.gz">
            <JsonLayout compact="true" eventEol="true" properties="true" />
            <Policies>
                <TimeBasedTriggeringPolicy interval="1"/>
                <SizeBasedTriggeringPolicy size="10MB"/>
            </Policies>
        </RollingFile>

    </Appenders>

    <!-- === Loggers por paquete o módulo === -->
    <Loggers>

        <!-- Tu aplicación -->
        <Logger name="com.celesoft" level="debug" additivity="false">
            <AppenderRef ref="Console" />
            <AppenderRef ref="FileAppender" />
        </Logger>

        <!-- R2DBC -->
        <Logger name="org.springframework.r2dbc" level="warn" additivity="false">
            <AppenderRef ref="Console" />
        </Logger>

        <!-- WebFlux -->
        <Logger name="org.springframework.web" level="info" additivity="false">
            <AppenderRef ref="Console" />
        </Logger>

        <!-- SQL y PostgreSQL -->
        <Logger name="io.r2dbc.postgresql" level="warn" additivity="false">
            <AppenderRef ref="Console" />
        </Logger>

        <!-- Root global -->
        <Root level="info">
            <AppenderRef ref="Console" />
            <AppenderRef ref="FileAppender" />
            <AppenderRef ref="JsonAppender" />
        </Root>

    </Loggers>
</Configuration>
